# FORGE-EXT-002 — Hallam recovery and live lesson application

**Status:** REDIRECTED
**Repository:** Forge `nrsandoval1231-oss/forge-agent`; target `nrsandoval1231-oss/hallam`
**Forge base:** `0e418b405e1f79312d5a281fd76d3278e7244f90`
**Hallam base:** `2d687fe51fdc406c35f52cfc8c275615f2792772`
**External branch:** `codex/forge-external-trial`
**Dependencies:** `FORGE-EXT-DIAG-001` COMPLETE; `FORGE-EXT-LESSON-001` COMPLETE
**Authority:** A3 for the owner-selected Hallam branch; A2 for one additional DeepSeek request under the owner's 2026-09-22 up-to-$15 total authorization for the remaining live self-improvement proof
**Risk:** HIGH because this packet permits one paid provider effect and one remote branch push after audit
**Retry budget:** no automatic provider retry; two local implementation repairs before the call

## Objective

Use the validated repository-scoped lesson
`LESSON-EXPLAIN-EXTERNAL-REFUSAL` to change the later execution strategy, make
one materially different proposal-only request, repair Hallam's Industry filter
if the proposal validates, independently audit both exact candidates, and push
only the named Hallam branch. Keep diagnostic improvement and task success as
separate measurements.

## Allowed files

- `scripts/external_trial_recovery.py`
- `tests/test_external_trial_recovery.py`
- `.agent/artifacts/FORGE-EXT-002/**`
- `.agent/ledger/FORGE-EXT-002.*`
- `.agent/tasks/FORGE-EXT-002.md`
- `.agent/graph/work-graph.json`
- `.agent/CURRENT_STATE.md`

Sol alone may later apply an accepted proposal to:

- Hallam `apps/api/sabra_api/main.py`
- Hallam `tests/test_api.py`

## Exclusions

No mutation of the retained EXT-001 stream, no retry under its claim, no other
Forge or Hallam file, no `main` push, merge, deployment, application submission,
candidate data, credential output, raw response persistence, global lesson
promotion, protected-policy change, or self-certification.

## Required behavior

1. Reconstruct the validated lesson and independent decision through the shipped
   Learning Engine. Retrieve it for the real EXT-002 failure signature.
2. Record an `ApplicationRecord` before the call. It must derive that the
   retrieved lesson changed the strategy from the original generic-refusal runner
   to the independently accepted safe-diagnostic recovery runner.
3. Use a materially different, narrower structured proposal contract. The model
   selects the Industry search fields and the positive/negative behavior cases;
   trusted code validates those choices and deterministically constructs only the
   two allowed Hallam edits. The model may not supply arbitrary file content.
4. Preserve the accepted credential scanning, refusal codes, bounded metadata,
   one-call claim, durable intent/checkpoint reload, identity checks, request
   bounds, measured latency, and `UNKNOWN` behavior.
5. The packet permits exactly one additional request and no automatic retry.
   EXT-001 plus EXT-002 conservative maxima must be at most $0.46432, below the
   owner's $15 total ceiling. Actual billing remains UNKNOWN unless returned.
6. On success, Sol reviews and applies only the deterministic edits. Prove an
   Industry-only match and unrelated-industry non-match, run Hallam focused tests,
   Ruff, formatting, and full tests, and report the known live Greenhouse failure
   separately if it remains.
7. Measure `unexplained_external_refusals` after the result. A validated proposal
   or independently reconstructable safe refusal observes zero; an interrupted or
   ambiguous effect is UNKNOWN. This metric does not substitute for Hallam task
   success.
8. Freeze exact Forge and Hallam candidates. Astra must accept the exact
   candidates before the named branch push. Verify the remote SHA after push.

## Acceptance

- The lesson is retrieved and changes the recorded strategy before the request.
- One request produces a valid checkpointed receipt chain with no sensitive
  response content.
- The Hallam Industry filter satisfies positive and negative boundary behavior.
- The Hallam diff contains only the two allowed files.
- Local Hallam validation and an exact-candidate independent audit pass.
- Remote `codex/forge-external-trial` resolves to the audited Hallam SHA.
- The Learning Engine measures the named diagnostic metric from real evidence;
  external-trial success is reported independently.

## Handoff

Return exact commands, exit codes, SHAs, changed files, application/retrieval
digests, request/response digests, sanitized usage, ledger/checkpoint heads,
measurement, audit verdict, remote branch SHA, spend status, and unknowns. Do
not self-certify, merge, or deploy.
