# FORGE-EXT-LESSON-001 — validate the external-refusal lesson

**Status:** AWAITING_AUDIT
**Repository:** `nrsandoval1231-oss/forge-agent`
**Base:** `2db548693bcace2606e459b962571b6b47960e81`
**Dependencies:** `FORGE-EXT-001` REDIRECTED; `FORGE-EXT-DIAG-001` independently ACCEPTED
**Authority:** A3 learning-governance packet under the owner's V0 completion direction
**Risk:** MEDIUM because an accepted lesson may shape a later paid packet
**Retry budget:** one evidence correction; no change to the lesson's meaning

## Objective

Independently validate or reject one repository-scoped candidate lesson derived
from the real `FORGE-EXT-001` refusal. Do not apply or measure it in this packet.

## Allowed files

- `.agent/tasks/FORGE-EXT-LESSON-001.md`
- `.agent/artifacts/FORGE-EXT-LESSON-001/**`
- `.agent/graph/work-graph.json`
- `.agent/CURRENT_STATE.md`

## Exclusions

No provider request, implementation change, Hallam edit, ledger rewrite, retry,
push, merge, deployment, global lesson scope, protected-policy change, or
self-improvement completion claim.

## Candidate lesson

When a paid external Builder proposal can be refused after receipt, the execution
strategy must use independently audited, allowlisted refusal reason codes and
content-free hunk metadata, with credential scanning before response-derived
persistence. A generic exception type without the failed rule prevents
attribution and makes blind retry unsafe.

The named metric is `unexplained_external_refusals`, lower is better. The real
baseline is one: `FORGE-EXT-001` ended in a schema-valid refusal whose exact rule
could not be reconstructed. A later packet may count zero only if it returns a
validated proposal or a refusal with an independently reconstructable safe reason.
An interrupted or ambiguous effect remains UNKNOWN.

## Acceptance

- The candidate artifact re-derives through the shipped Learning Engine types.
- Outcome provenance binds exact Forge candidate, Hallam base, strategy, packet,
  three-receipt ledger head, and the baseline metric.
- Astra is distinct from the author and independently validates or rejects the
  diagnosis, scope, metric, negative knowledge, and intended change.
- An accepted decision is repository-scoped only and cites exact audit evidence.
- No later application or improvement is claimed here.

## Handoff

Return ACCEPT or REJECT for the lesson, exact candidate identity, evidence digest,
scope, metric assessment, and any correction. Do not implement, call, push, merge,
deploy, or claim self-improvement.
