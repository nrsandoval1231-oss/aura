F
================================== FAILURES ===================================
___________ test_rejects_files_created_by_a_check_and_check_timeout ___________

tmp_path = WindowsPath('C:/Users/nrsan/AppData/Local/Temp/pytest-of-nrsan/pytest-2561/test_rejects_files_created_by_0')

    def test_rejects_files_created_by_a_check_and_check_timeout(tmp_path):
        repo = tmp_path / "repo"
        base = repo_at(repo)
        edit = {
            "tests/test_candidate.py": (
                "from pathlib import Path\n"
                "def test_adds_unapproved_file():\n"
                "    Path('surprise.txt').write_text('extra')\n"
            )
        }
        with pytest.raises(ForgeError, match="changed|scope"):
            execute_proposal(
                repo,
                Proposal("extra", base, edit),
                allowed_paths=tuple(edit),
                checks=(Check("pytest"),),
                receipt_root=tmp_path / "receipts-extra",
                worktree_root=tmp_path / "worktrees-extra",
            )
    
        repo2 = tmp_path / "repo-timeout"
        base2 = repo_at(repo2)
        slow = {"tests/test_candidate.py": "import time\ndef test_slow():\n    time.sleep(2)\n"}
>       with pytest.raises(ForgeError, match="timed out"):
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AssertionError: Regex pattern did not match.
E         Expected regex: 'timed out'
E         Actual message: 'Total check budget exhausted'

tests\test_aura_executor.py:402: AssertionError
=========================== short test summary info ===========================
FAILED tests/test_aura_executor.py::test_rejects_files_created_by_a_check_and_check_timeout
1 failed in 7.00s
EXIT_CODE=1
