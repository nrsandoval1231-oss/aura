# PR #13 candidate-binding review repair

- Objective: prevent `AWAITING_AUDIT` packets from escaping the default exact-candidate slice check. Independent review found five such packet records while `scripts/run_slice.py --check` selected zero.
- Repository: `nrsandoval1231-oss/forge-agent`; isolated worktree `C:/Users/nrsan/.codex/worktrees/forge-pr13-binding-repair/forge-agent`; branch `codex/forge-pr13-binding-repair`; integration base `621d8d2d503aed037afb0b87698b3e433486aeb7`, tree `a7c18dd837eb0cb005ec8eb4c908b514dae7d967`.
- Node: repair of PR #13's unfinished candidate-binding evidence; the existing `FORGE-FIX-004` and following packets remain `AWAITING_AUDIT`. This packet does not certify them or change graph status.
- Authority: owner instructed autonomous V0 completion. This is validation authority, so preserve fail-closed behavior and exact candidate identity; no canonical requirements, trust kernel, evidence ledger, graph, merge, or deployment changes.
- Owned files: `scripts/run_slice.py`, `tests/test_slice_contexts.py` and/or `tests/test_gate_parity.py`, plus completion evidence under `.agent/artifacts/FORGE-FIX-004/`. Propose any other path to Sol before editing. Read `AGENTS.md`, `.agent/CURRENT_STATE.md`, `.agent/AGENT_RULES.md`, graph and relevant packets first.
- Invariants: pending independent audit is still an active candidate; a mutation of a candidate before audit cannot pass the default validation gate; historical evidence is append-only and is never silently rebound or rewritten. An earlier packet on a sequential branch may be audited at its exact retained commit, but that audit cannot certify later changed content. Missing candidate/evidence is UNKNOWN or failure, never PASS.
- Acceptance: first reproduce the zero-target default check against this base and a changed file after recorded binding. Add focused tests for selection of pending packet IDs, mismatch/missing evidence, and no false historical certification. Make the default check fail closed on PR #13's stale pending bindings until exact candidate audit and governance resolution. Preserve existing COMPLETE packet ledger verification and ordinary READY/IN_PROGRESS behavior. Avoid automatic `--rebind` or rewriting prior receipts.
- Gates: focused tests, Ruff, diff check; report the expected pending-audit binding failure distinctly. Sol will run all applicable checks and independently audit integrated work. The Windows executable-bit test is separate.
- Retry budget: two materially different bounded repairs. Return to Sol if no safe treatment of sequential pending candidates exists within these files.
- Handoff: exact changed files, failing-before and passing-after tests with exit codes, default check output, unknowns, and an implementation claim only. Do not commit, push, merge, deploy, spend, change graph status, or edit the other builder's learning files. You are one of two disjoint builders.

## Independent audit FIX — bounded correction 1

Exact candidate `d563cf193e648879c2b08a54362dce31387f188e` was independently audited. Two fail-open paths remain in the binder: `active_packet_ids()` silently skips active graph nodes when the task file named by their `packet` reference is missing; default `--check` then returns success for zero targets. This also misses active nodes intentionally sharing a packet (STK-001/STK-003/LRN-001 share FORGE-LRN-000). Resolve active node `packet` references, deduplicate packet IDs, and fail on missing or invalid references; no active graph node may disappear from validation. Second, absent ledger `AUDIT_RECORDED` or `MERGE_ELIGIBILITY_GRANTED` receipts currently pass conditional comparisons. Require actual receipts in the intended order bound to the candidate, not a boolean in SLICE.json. Reproduce both with focused tests before correction. Keep historical ledgers and graph unchanged. Correct the misleading `--rebind` error suggestion if it encourages destructive history replacement. Report exact gates; do not commit or push.
