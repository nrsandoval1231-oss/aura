# AURA-M1-EXEC-001 scope note

This packet proves the Git isolation, scope, check, and candidate-binding mechanism with an explicitly supplied test proposal. It does not supply a substitute model for the owner-selected DeepSeek builder. Production `aura build` stays blocked pending verified provider wiring, budget authority, owner-pinned review trust, and effect reconciliation.

The executor must return a review request, never an approval. The existing simulated `run_slice.py` receipt is mechanism evidence only.

The current detached verifier is Forge-specific: it requires the `forge-agent.detached-audit.v1` domain and `FORGE-INTEGRATED-001` validation artifact. An Aura-specific trust packet must update that protected verifier and validation binding under independent review before any Aura public key is pinned. Merely filling `.agent/audit-trust.json` would not certify an Aura candidate. The owner needs to approve the eventual public-key fingerprint; the private key must remain with the independent auditor outside builder and controller worktrees.
